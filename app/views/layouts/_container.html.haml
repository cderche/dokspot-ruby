-if state == 1
  %body
    #wrap
      %header
        = render 'layouts/navigation'
      .container
        = render 'layouts/messages'
      = yield
      #push
    = render 'layouts/footer'
-elsif state == 2
  %body
    #wrap
      %header
        = render 'layouts/navigation'
      .container
        = render 'layouts/messages'
      = yield
      #push
    = render 'layouts/footer'
-else
  %body
    #wrap
      %header
        = render 'layouts/navigation'
      .container
        = render 'layouts/messages'
        = yield
      #push
    = render 'layouts/footer'