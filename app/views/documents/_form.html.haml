= simple_form_for [@instruction, @document] do |f|
  = f.error_notification

  .form-inputs
    .form-group
      = f.input :version, input_html: { class: 'form-control', placeholder: 'e.g. 1.0', autofocus: true}
    %br
    .form-group
      = f.file_field :file
      %p.help-block
        Upload a file in 
        %strong
          = @instruction.language.name
        for product
        %strong
          = @instruction.product.name
    %br
    .form-group
      = f.check_box :primary
      = f.label :primary

    %br
    .form-actions
      .text-center  
        = f.button :submit, class: 'btn-success'