- if policy(user).destroy?
  %button.btn.btn-danger.btn-xs{data: {toggle: 'modal', target: "#deleteUser#{user.id}"}}
    = t('keywords.delete')
  
  .modal.fade{id: "deleteUser#{user.id}", tabIndex: '-1', role: 'dialog', aria: {labelledby: "deleteUser#{user.id}", hidden: true}}
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{data: {dismiss: 'modal'}, type: 'button'}
            %span{aria:{hidden:true}} &times;
            %span.sr-only Close
          %h4.modal-title.text-danger.text-center
            = t('keywords.warning')
        .modal-body.text-left
          %p= t('users.destroy.warning', email: user.email)
        .modal-footer
          %button.btn.btn-default{data:{dismiss:'modal'}} Close
          = link_to t('keywords.delete'), user, method: :delete, class: 'btn btn-danger'